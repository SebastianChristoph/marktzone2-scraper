services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - SCRAPER_DATA_DIR=/app/data
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "5174:80"
    depends_on:
      - backend
